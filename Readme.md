# 基于Unicorn Engine的CBE模拟器
- 可以运行酷吧游戏

# 目录建议
- `src/` 模拟器与平台兼容层
- `tools/` 反汇编、提取、校验等脚本
- `docs/re/` 固件、协议、运行时约定、会话记录
- `firmware/` 固件原件、解包结果、manifest
- `samples/` 抓包、截图、测试样本
- `server/` 后续私服实现

# 编译工具
- mingw32

# 编译

- 双击build.bat文件
