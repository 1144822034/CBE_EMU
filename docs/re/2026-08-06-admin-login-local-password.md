# 后台登录页：本地记住密码

后台登录仍以服务端 MySQL 配置验证密码，并继续使用原有失败计数与五次锁定规则。
“记住密码”不是服务端会话延长，也不会把密码写入数据库或 Cookie。

登录页加载一个不需要认证、无网络请求的同源脚本。管理员主动勾选时，脚本仅在当前
浏览器配置文件的 `localStorage` 中写入密码；下次打开登录页时回填。取消勾选会立即
删除该键。清除该站点浏览器数据也会删除密码。

登录脚本只包含本地读取/写入逻辑，响应中不含管理密码，也不读取或修改 MySQL 数据。
